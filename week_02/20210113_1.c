/*
Упражнение 1:
Съберете char (255 + 10).
▪ Какво ще се получи?
▪ Защо?
*/

#include <stdio.h>

int main(){

    char a = 255;
    char b = 10;
    char total = a + b;

    printf("%d \n", total);
    printf("\nТъй като типът 'char' приема стойности до '255', при добавянето на още 10 го нулира и започва да отброява до като достигне до десет т.е. от '0 - 9'. Поради тази особеност резултатът е '9'.\n");

return 0;
}
